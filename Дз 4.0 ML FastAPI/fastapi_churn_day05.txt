<b>День 5 — Обучение базовой модели churn классификации</b>

🎯 <b>Цель дня</b>
Реализовать первый ML pipeline для прогнозирования churn.

📋 <b>Задачи</b>
1. 🤖 Выбрать простую модель например <code>LogisticRegression</code> из scikit learn

2. ⚙️ Настроить предобработку признаков:
   - 📏 масштабирование числовых признаков
   - 🔤 кодирование категориальных через <code>OneHotEncoder</code>

3. 🔗 Объединить предобработку и модель в единый <code>Pipeline</code> или использовать <code>ColumnTransformer</code>

4. 🎓 Реализовать функцию <code>train_churn_model</code> которая принимает тренировочный датасет и возвращает обученный pipeline

5. 🔗 Добавить эндпоинт <code>POST /model/train</code> который запускает обучение модели на данных из <code>churn_dataset.csv</code> и возвращает базовые метрики <code>accuracy</code> и <code>f1</code> на тестовой выборке

6. ⚠️ Обработать ситуацию когда датасет ещё не загружен или пуст

---
<i>Удачи с реализацией!</i> 🚀