<b>День 9 — Улучшенная предобработка churn признаков</b>

🎯 <b>Цель дня</b>
Привести предобработку churn данных к более аккуратному и универсальному виду.

📋 <b>Задачи</b>
1. 🔢 Явно разделить признаки churn на числовые и категориальные

2. 🔧 Использовать <code>ColumnTransformer</code> и <code>Pipeline</code> чтобы объединить:
   - <code>StandardScaler</code> для числовых признаков
   - <code>OneHotEncoder</code> для категориальных признаков

3. ✅ Убедиться что вся цепочка предобработки и модель сохраняются и загружаются как один объект

4. 🔄 Обновить логику подготовки <code>FeatureVectorChurn</code> для предсказания так чтобы порядок и набор признаков точно совпадал с обучением

5. 🔍 Добавить эндпоинт <code>GET /model/schema</code> который возвращает список признаков <code>churn</code> и ожидаемые типы значений чтобы клиент мог правильно формировать запросы

---
<i>Удачи с реализацией!</i> 🚀