<b>Общая идея проекта</b>

Вы разрабатываете учебный сервис на Python и FastAPI для решения конкретной задачи машинного обучения. Задача сервиса — предсказывать отток клиентов <b>churn</b> на основе их поведения и характеристик.

🎯 <b>Конкретная задача классификации</b>
Клиент может уйти из сервиса в следующем месяце или остаться. Целевая переменная <code>churn</code> принимает значение <b>1</b> если клиент ушёл, <b>0</b> если остался.

💾 Сервис работает на основе табличных данных. Для обучения модели используется искусственно сгенерированный датасет в файле <code>churn_dataset.csv</code> который входит в это домашнее задание.

📊 <b>Признаки в датасете</b>
1. 💰 <code>monthly_fee</code> — ежемесячная стоимость тарифа клиента
2. ⏰ <code>usage_hours</code> — количество часов использования сервиса за последний месяц
3. 🎧 <code>support_requests</code> — количество обращений в техподдержку
4. 📅 <code>account_age_months</code> — возраст аккаунта в месяцах
5. ❌ <code>failed_payments</code> — количество неудачных платежей
6. 🌍 <code>region</code> — регион клиента (<i>europe, asia, america, africa</i>)
7. 💻 <code>device_type</code> — основной тип устройства (<i>mobile, desktop, tablet</i>)
8. 💳 <code>payment_method</code> — способ оплаты (<i>card, paypal, crypto</i>)
9. 🔄 <code>autopay_enabled</code> — включено ли автосписание (<i>0 или 1</i>)
10. 🎯 <code>churn</code> — целевой признак (<i>1 клиент ушёл, 0 клиент остался</i>)

🚀 <b>К концу второй недели сервис должен уметь</b>
1. 📥 Загружать тренировочный датасет через API или использовать локальный <code>churn_dataset.csv</code>
2. ⚙️ Проводить предобработку данных и разбиение на train и test
3. 🎓 Обучать модель классификации churn на основе scikit learn
4. 💾 Сохранять и загружать обученную модель
5. 🎯 Принимать новые объекты и выдавать предсказание churn и вероятности
6. 📊 Показывать базовые метрики качества модели
7. 🧪 Иметь базовые тесты и запускаться в Docker

📅 Ниже каждая из двух недель разбита по дням. Каждый день представляет отдельный шаг в построении полноценного ML сервиса.

---
<i>Удачи с реализацией!</i> 🚀