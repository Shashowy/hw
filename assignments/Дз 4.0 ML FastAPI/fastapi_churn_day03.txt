<b>День 3 — Загрузка и предварительный просмотр churn датасета</b>

🎯 <b>Цель дня</b>
Научиться работать с тренировочным датасетом <code>churn_dataset.csv</code>.

📋 <b>Задачи</b>
1. 📁 Положить файл <code>churn_dataset.csv</code> в папку проекта например в папку <code>data</code>

2. 🔧 Добавить модуль для работы с датасетом который умеет:
   - 📖 читать CSV файл
   - 🔄 преобразовывать строки в объекты <code>DatasetRowChurn</code>
   - 💾 хранить данные в pandas DataFrame или в списке словарей

3. 🔗 Добавить эндпоинт <code>GET /dataset/preview</code> который возвращает первые N строк из <code>churn_dataset.csv</code> в виде JSON

4. 📊 Добавить эндпоинт <code>GET /dataset/info</code> который показывает:
   - 📏 количество строк
   - 📐 количество столбцов
   - 📝 список названий признаков
   - 📈 распределение переменной <code>churn</code> по классам

5. ✅ Убедиться что загрузка и просмотр датасета не приводит к ошибкам

---
<i>Удачи с реализацией!</i> 🚀